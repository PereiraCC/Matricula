@page "/Mantenimiento/Register_Horarios"
@model RegistrarHorariosModel
@{
    ViewData["Title"] = "Registro Horarios";
}

<html>
<head>
    @*<link rel="stylesheet" href="~/css/Register.css" />
        <script type="text/javascript" src="~/js/Registrar.js"></script>*@
</head>
<body>
    <div class="container p-4">
        <form method="post" enctype="multipart/form-data" autocomplete="off">
            <div class="col-xs-6 col-md-5">
                <div class="panel panel-primary">
                    <div class="panel-heading">
                        <h3 class="panel-title">@ViewData["Title"]</h3><br />
                    </div>
                    <div class="panel-body">
                        <div class="accordion" id="accordionExample">
                            <div class="card">
                                <div class="card-header" id="headingOne">
                                    <div id="header" class="bg-info">
                                        <h2 class="mb-0 t">
                                            <button class="btn btn-link text-light" type="button" data-toggle="collapse"
                                                    data-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                                                Ingresar Informacion
                                            </button>
                                        </h2>
                                    </div>
                                </div>
                                <div id="collapseOne" class="collapse show" aria-labelledby="headingOne"
                                     data-parent="#accordionExample">
                                    <div class="card-body">
                                        @if (Model.Input_Horarios.Codigo_Horario == null)
                                        {
                                            <div class="form-group">
                                                <input asp-for="Input_Horarios.Codigo_Horario" placeholder="Codigo Horario" class="form-control" autofocus />
                                                <span asp-validation-for="Input_Horarios.Codigo_Horario" class="text-danger"></span>
                                            </div>
                                        }
                                        else
                                        {
                                            <div class="form-group">
                                                <input asp-for="Input_Horarios.Codigo_Horario" placeholder="Codigo Horario" class="form-control" autofocus readonly />
                                                <span asp-validation-for="Input_Horarios.Codigo_Horario" class="text-danger"></span>
                                            </div>
                                        }
                                        @if (Model.Input_Horarios.Dia == null)
                                        {
                                            <div class="form-group">
                                                @Html.DropDownList("Input_Horarios.Dia", new SelectList(Model.Input_Horarios.dias, "Text", "Text"), "Seleccione un día", new { @class = "form-control" })
                                                <span asp-validation-for="Input_Horarios.Dia" class="text-danger"></span>
                                            </div>
                                        }
                                        else
                                        {
                                            <div class="form-group">
                                                @Html.DropDownList("Input_Horarios.Dia", new SelectList(Model.Input_Horarios.dias, "Text", "Text"), Model.Input_Horarios.Dia, new { @class = "form-control" })
                                                <span asp-validation-for="Input_Horarios.Dia" class="text-danger"></span>
                                            </div>
                                        }

                                        @if (Model.Input_Horarios.Hora_Inicial == null)
                                        {
                                            <div class="form-group">
                                                @Html.DropDownList("Input_Horarios.Horas_Iniciales", new SelectList(Model.Input_Horarios.Horas_Iniciales, "Text", "Text"), "Seleccione un Hora Inicial", new { @class = "form-control" })
                                                <span asp-validation-for="Input_Horarios.Horas_Iniciales" class="text-danger"></span>
                                            </div>
                                        }
                                        else
                                        {
                                            <div class="form-group">
                                                @Html.DropDownList("Input_Horarios.Horas_Iniciales", new SelectList(Model.Input_Horarios.Horas_Iniciales, "Text", "Text"), Model.Input_Horarios.Hora_Inicial, new { @class = "form-control" })
                                                <span asp-validation-for="Input_Horarios.Horas_Iniciales" class="text-danger"></span>
                                            </div>
                                        }
                                        @if (Model.Input_Horarios.Hora_Final == null)
                                        {
                                            <div class="form-group">
                                                @Html.DropDownList("Input_Horarios.Horas_Finales", new SelectList(Model.Input_Horarios.Horas_Finales, "Text", "Text"), "Seleccione un Hora Final", new { @class = "form-control" })
                                                <span asp-validation-for="Input_Horarios.Horas_Finales" class="text-danger"></span>
                                            </div>
                                        }
                                        else
                                        {
                                            <div class="form-group">
                                                @Html.DropDownList("Input_Horarios.Horas_Finales", new SelectList(Model.Input_Horarios.Horas_Finales, "Text", "Text"), Model.Input_Horarios.Hora_Final, new { @class = "form-control" })
                                                <span asp-validation-for="Input_Horarios.Horas_Finales" class="text-danger"></span>
                                            </div>
                                        }

                                        <div class="form-group text-center">
                                            <button type="submit" class="btn btn-primary btn-block">Registrar</button>
                                        </div>
                                        <div class="form-group">
                                            <label class="text-danger">@Html.DisplayFor(m => m.Input_Horarios.ErrorMessage)</label>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </form>
    </div>
</body>
</html>